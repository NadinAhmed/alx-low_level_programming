#include "main.h"

/**
 * mul -> multiple of two given number
 *
 * @a: first number
 * @b: second number
 *
 * Return: the multiple of two numbers
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}

#include "main.h"

/**
 * _abs -> check adsolute given number
 *
 * @i: The number you want to check
 *
 * Return: absolute value of i
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	return (i);
}
